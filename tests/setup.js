/**
 * setup.js — Configuración global del entorno de testing
 *
 * Este archivo se ejecuta antes de cada suite de tests.
 * Usarlo para configurar jsdom, extender matchers de Vitest,
 * o definir globales necesarios para los tests.
 *
 * Ejemplo de uso futuro:
 *   import '@testing-library/jest-dom';  // matchers adicionales de DOM
 */
